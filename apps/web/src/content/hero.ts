/** The hero is a second "CoachLens" moment right after the intro — the
 *  giant wordmark dead-centre, the bowler photo dimmed in behind it as
 *  texture rather than a foreground image. No headline, no subtitle: the
 *  brand carries the section on its own, the way ethnocare's product name
 *  carries theirs. */
export const HERO = {
  wordFirst: 'Coach',
  wordSecond: 'Lens',
  image: {
    src: '/images/hero-bg.jpg',
    alt: 'Fast bowler mid-delivery at the nets',
    width: 1200,
    height: 1500,
  },
} as const;
