# Same Supabase project as services/coaching-api's SUPABASE_URL/SUPABASE_KEY —
# the anon/publishable key here, never the service-role key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# services/coaching-api's port (see .claude/launch.json's "coaching-api" entry).
VITE_API_BASE_URL=http://localhost:8811
